services:
  cookie-injector:
    build: .
    container_name: cookie-injector
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./data:/data
    extra_hosts:
      - "browserless:172.20.0.2"
